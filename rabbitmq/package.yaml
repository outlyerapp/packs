title: RabbitMQ
author: Dataloop.IO
version: 1.1.0
processes:
    - rabbitmq-server
description: Scrapes the management plugin to return overview statistics. Optionally returns queue and exchange level statistics.
instructions_required: true
icon:
    name: rabbitmq
    background: orange
    foreground: white
